import { CustonInputDirective } from './custon-input.directive';

describe('CustonInputDirective', () => {
  it('should create an instance', () => {
    const directive = new CustonInputDirective();
    expect(directive).toBeTruthy();
  });
});
