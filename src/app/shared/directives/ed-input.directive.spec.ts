import { EdInputDirective } from './ed-input.directive';

describe('EdInputDirective', () => {
  it('should create an instance', () => {
    const directive = new EdInputDirective();
    expect(directive).toBeTruthy();
  });
});
